---
id: T-001
title: Revoke leaked Discogs token and purge from history
type: bug-fix
lens: bug-fix
created: 2026-05-19
status: ready
acceptance:
  - Old Discogs PAT revoked on Discogs API settings
  - Old token removed from git history via git filter-repo
  - config.yaml points to .env for secret (no plaintext value)
  - git log --all contains no trace of old token (grep for nPJEBKAk)
  - Old token returns 401 from Discogs API endpoint
files:
  touched:
    - photo_project/src/config.yaml
    - .gitignore
    - git history
  must-not-touch:
    - Everything else
---

# T-001: Revoke leaked Discogs token and purge from history

## Why
A real personal access token is committed in plaintext in `photo_project/src/config.yaml` in the public repo. The token has been revoked on the Discogs side; this task completes the remediation by purging it from git history and moving to `.env` storage.

## What
1. Rewrite git history to remove the leaked token from `photo_project/src/config.yaml` using `git filter-repo`
2. Update `.gitignore` to ensure config.yaml is never tracked again
3. Verify the old token is completely gone from `git log`
4. Confirm old token returns 401 from Discogs API

## Out of scope
- T-002 (.gitignore root fix) — separate task
- T-003 (config.example.yaml creation) — separate task, depends on this
- Rotating other API keys — covered by XR-002

## Notes
- User has already revoked the old token and obtained a new one
- New token will be stored in `.env` (not yet; T-003 handles that)
- Force-push to origin required; notify collaborators

## References
- FIXES.md T-001
